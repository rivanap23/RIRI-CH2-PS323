<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Riri" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorOnPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/primary_variant</item>
        <item name="colorPrimaryContainer">@color/container</item>
        <item name="color">@color/primary</item>
        <item name="colorSurfaceVariant">@color/primary</item>
        <item name="android:windowBackground">@color/white</item>
        <item name="android:windowActionBar">@color/white</item>
        <item name="bottomAppBarStyle">@style/Widget.App.BottomNavigationView</item>
        <item name="android:forceDarkAllowed" tools:targetApi="q">false</item>
    </style>

    <style name="Widget.App.BottomNavigationView" parent="Widget.Material3.BottomNavigationView">
        <item name="materialThemeOverlay">@style/ThemeOverlay.App.BottomNavigationView</item>
    </style>

    <style name="ThemeOverlay.App.BottomNavigationView" parent="">
        <item name="colorSecondaryContainer">@color/primary</item>
        <item name="colorOnSecondaryContainer">@color/white</item>
        <item name="colorSurface">@color/primary_variant</item>
        <item name="colorOnSurfaceVariant">@color/black</item>
    </style>

    <style name="TextAppearance.App.TopAppBar" parent="TextAppearance.AppCompat.Title">
        <item name="materialThemeOverlay">@style/ThemeOverlay.App.Title.TopAppBar</item>
    </style>

    <style name="ThemeOverlay.App.Title.TopAppBar" parent="">
        <item name="android:textSize">22sp</item>
        <item name="fontFamily">@font/inter_bold</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="Theme.Riri" parent="Base.Theme.Riri" />
</resources>